{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Install deps (web)",
      "type": "shell",
      "command": "npm install",
      "options": { "cwd": "${workspaceFolder}/web" }
    },
    {
      "label": "Dev (web)",
      "type": "shell",
      "command": "npm run dev",
      "options": { "cwd": "${workspaceFolder}/web" },
      "problemMatcher": "$tsc"
    }
  ]
}
